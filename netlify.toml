[build]
  command = "npm run build"
  publish = "dist"

# 실제 파일(JS/CSS 등)이 있으면 그걸 우선 제공하고,
# 없는 경로만 SPA용으로 index.html로 보냅니다.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
